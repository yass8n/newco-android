<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.newco.newco_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SessionListActivity"
            android:label="@string/title_activity_session_list">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                techic
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.UsersListActivity"
            android:label="@string/title_activity_attendees_list" />
        <activity
            android:name=".activities.SessionTypeListActivity"
            android:label="@string/title_activity_session_type_list" />
        <activity
            android:name=".activities.DirectoryActivity"
            android:label="@string/title_activity_directory" />
        <activity
            android:name=".activities.SessionInfoActivity"
            android:label="@string/title_activity_session_info" />
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".activities.UserInfoActivity"
            android:label="@string/title_activity_user_info" />
        <activity
            android:name=".activities.VenueInfoActivity"
            android:label="@string/title_activity_venue_info" />
        <activity
            android:name=".activities.VenuesListActivity"
            android:label="@string/title_activity_venues_list" />
        <activity
            android:name=".activities.SearchResultsActivity"
            android:label="@string/title_activity_search_results"></activity>
    </application>

</manifest>
